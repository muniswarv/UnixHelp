
set Var = One;

if ( $Var == "One" ) then 
    echo "  $Var equals One"
else 
    echo "  $Var not equals One"
endif

if ( $Var == "Two" ) then 
    echo "  $Var equals Two"
else 
    echo "  $Var not equals Two"
endif

